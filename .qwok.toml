# qwok app definition: how this project runs (read by `qwok run`).
name = "deep-time"
cmd  = "sh -c \"python3 -m http.server $PORT\""
# type = "app"   # set for desktop/non-web projects (run directly, no portless/URL)
# app_port = 3000   # optional: pin a fixed port (default: portless auto-assigns)

# [env]
# DEBUG = "1"
